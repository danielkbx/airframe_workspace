# Flight Controller Connectivity

- Status: active
- Last reviewed: 2026-08-06
- Scope: Betaflight MSP/CLI transports, serial and BLE behavior, FlashFS, mass storage, helper hardware, and real-device validation
- Normative decisions: `../.agents/MEMORY.md`
- Related implementation: Airframe flight-controller import packages and app flows

## Evidence Matrix

| ID | Claim | Source | Grade | Scope | Airframe impact | Status |
|---|---|---|---|---|---|---|
| FCC-001 | Configurator treats serial, Web Bluetooth, and native BLE as byte transports beneath common MSP/CLI layers. | BFC | B | Pinned Configurator revision | Keep transports isolated from protocol/session logic. | verified |
| FCC-002 | SpeedyBee BLE advertisements cannot be relied on to expose a recognized service/name; service validation must occur after connection. | Local hardware tests, BFC | A/B | Adapter 3 | Publish discovery callbacks and validate profiles post-connect. | verified |
| FCC-003 | FlashFS download uses MSP 70/71; current source exposes SD summary but no equivalent MSP SD-file read. | BF-SRC | A | Current firmware revision | Direct MSP import is FlashFS-only; SD logs need filesystem/mass-storage access. | verified |
| FCC-004 | Sandboxed macOS serial access works with `com.apple.security.device.serial`; USB entitlement was unnecessary in tested hardware. | Local tests | A | macOS tested devices | Preserve the serial entitlement and IOKit/callout design. | verified |
| FCC-005 | iPadOS user-space CDC-ACM is unavailable under the no-DriverKit/no-MFi/no-private-API constraints. | SDK probes, Apple docs | A | iPadOS/Xcode 26.5 | Prefer BLE, mass storage through Files, or external helper architecture. | verified |
| FCC-006 | A live device advertised as `TMOTORFC_00EUV` exposes services `AE00` and `FFE0`; the transparent UART surface under `FFE0` has one shared `FFE1` write/notify characteristic rather than the split CC2541 `FFE1`/`FFE2` pair. | Local Airframe CoreBluetooth log | A | Tested T-Motor FC, 2026-08-06 | Resolve same-service BLE profiles by discovered characteristic layout and properties, not device name or service alone. | verified through full app download |
| FCC-007 | T-Motor's user guidance says the built-in Bluetooth link requires MSP enabled on the last Betaflight port, UART6, and that reflashing can leave it disabled. The tested FC initially had UART6 MSP disabled; after it was enabled and the client-side first-request race was mitigated, MSP identity, dataflash summary, and download all succeeded over `FFE1`. | TM-GUIDE, local hardware test | A/B | T-Motor/T-Hobby Bluetooth FC guidance plus tested device | When this board resolves its BLE UART but cannot complete MSP, tell the user to verify that MSP is enabled on UART6 before treating the transport as incompatible. | guidance and live UART6 requirement verified |
| FCC-008 | The tested controller is likely the VELOX F7 SE: T-Hobby's official product page explicitly advertises built-in Bluetooth parameter adjustment through the SpeedyBee mobile app. After UART6 MSP was enabled, one Airframe `FFE0 UART` session completed the identity handshake and dataflash summary, while a fresh app launch immediately afterward reached the same BLE profile but timed out on the first MSP request; Betaflight Configurator also fails to connect over Bluetooth. | TM-F7-SE, local hardware test | A/B | User-identified board model, not independently confirmed from its physical markings | UART6 MSP is relevant but not sufficient evidence of a stable link. Treat the behavior as intermittent controller/module interoperability until repeated handshakes and a download succeed. | model likely; intermittent full handshake verified |
| FCC-010 | A minimal CoreBluetooth CLI probe (`tools/tmotor_ble_probe.swift`) completed the full identity handshake plus DATAFLASH_SUMMARY and a 512-byte DATAFLASH_READ against `TMOTORFC_00EUV` in 10 of 10 connect/disconnect sessions. BOARD_INFO confirms the board: identifier `S7X2`, target `TMOTORVELOXF7SE`, manufacturer `TMTR`, Betaflight 4.5.4, MSP API 1.46. The only fragility: the first MSP request after connect is sometimes answered ~1.6 s late or dropped when written immediately after notify enable; a ~500 ms delay before the first write, or a single retry, makes every session succeed. `maximumWriteValueLength` without response is 20 bytes. `FFE0` additionally exposes undocumented `FF02` (write) and `FF03` (write/notify) characteristics. | Local probe runs, 2026-08-06 | A | Tested VELOX F7 SE | The transport and controller are fine; the intermittent Airframe/Configurator failure is a client-side first-request race. Retry only the first idempotent MSP request instead of imposing a fixed delay or changing later request policy. | verified; mitigation implemented, live app acceptance pending |
| FCC-009 | BLE advertisements from the tested T-Motor can temporarily disappear during the device-selection step. The assistant previously replaced an expired selection with its preferred remaining device, favoring USB on macOS, so Next could connect a different transport without an intentional user selection. | Local UI observation and state audit | A | Flight-controller import discovery | Retain a selected device ID across transient scan absence and disable progression until it returns or the user explicitly chooses another device. | fixed and covered by assistant state tests |
| FCC-011 | Airframe completed the VELOX F7 SE `FFE0 UART` identity handshake, dataflash summary, and the full 38,912-byte Direct download, then saved and reopened a regular `.airframe` document. The one pre-existing FlashFS segment is malformed and yields zero readable logs: its header contains repeated fragments and `H Field P encod looptime:125`. An independent uncompressed live read and the saved document have the exact same first-1,024-byte SHA-256 (`3a11788ad7122062d6d11f21c40520b99fce5669cb8ce20806f4340743d7c565`), proving that the source FlashFS bytes were already damaged rather than corrupted by Airframe compression or container persistence. On the same FC, configuration capture succeeds over USB. A standalone framed `dump profile` over `FFE1` reproducibly disconnected after 1,018 CLI response bytes and left MSP forwarding unavailable until an FC power cycle, while framed `get gyro_high_range` completed with its ETX terminator and 53 response bytes in 0.04 seconds. A 1,024-byte stop-and-wait MSP dataflash response also succeeds. This isolates the limit to continuous unpaced FC-to-module CLI output rather than CoreBluetooth receive buffering or a generic BLE packet-size limit. `FF02` writes plus `FF03` notifications produced no MSP response and are not an alternate UART path. | Local Airframe Unified Log, saved document, standalone probe, and user hardware retest, 2026-08-06 | A | Tested VELOX F7 SE with Betaflight 4.5.4 | Full `dump all` cannot be made reliable by host-side receive throttling because Betaflight 4.5.4 emits the command synchronously without application-level flow control. Keep USB `dump all`; any BLE workaround must use bounded individual CLI/MSP requests and preserve as much raw configuration evidence as possible rather than silently substituting an Airframe-only subset. | transfer and byte integrity accepted; continuous CLI hardware limit verified; comprehensive segmented capture design pending |
| FCC-012 | A single BLE session successfully completed the BF 4.5.4 CLI-setting catalog in bounded exact `get` requests; the final 209-command segment transferred 12,929 response bytes with a maximum individual response of 430 bytes, and the preceding catalog segments were also stable. `get debug_mode` is the exception: its lookup list produced 873 bytes under an Airframe limit and 1,018 bytes after raising that limit, matching the bridge failure edge. The configured value itself is byte 18 of the already captured BF 4.5 `MSP_ADVANCED_CONFIG` response, so Airframe no longer sends that CLI request. Betaflight 2026.6 introduced value-only `MSP2_CLI_SETTING`; BF 4.5.5 and earlier do not have it. Airframe subsequently completed the production segmented path live on the T-Motor BF 4.5.4. Mass Storage captured 37,533 configuration bytes, recovered MSP, rebooted, copied one log from `BETAFLT`, and cleanly unmounted. Direct captured the same configuration size before downloading exactly 2,158,592 FlashFS bytes; only after completion did it erase, observe `usedBytes=0`, and reach ready after five polls. The user then completed the T-Motor USB/Bluetooth Direct/Mass Storage and SpeedyBee Adapter 3 Direct/Wi-Fi variants successfully. | Standalone probe, tagged Betaflight source audit, Airframe package/runtime tests, Unified Log, macOS build, and live import matrix, 2026-08-06 to 2026-08-07 | A | SpeedyBee V2; VELOX F7 SE on BF 4.5.4; tagged BF 4.3 through 2026.6 source | Select capture from the resolved GATT layout: the verified shared-`FFE1` profile uses comprehensive segmented capture; SpeedyBee V2 and other recognized profiles use canonical `dump all`; unknown capability remains segmented. Preserve the format marker/raw MSP records on constrained captures and never treat `diff all` as complete. | profile-based selection implemented and unit-tested; live revalidation pending; Wi-Fi delete capability mismatch remains |
| FCC-013 | Betaflight 4.5 has no `env`, and its real `status` syntax uses `MCU F722 Clock=...`, `Configuration: ...`, and comma-separated `GYRO=...` sensor assignments rather than the newer labels Airframe originally parsed. Because the observed continuous-response failure could depend on the BLE bridge, firmware, or their combination, neither firmware nor board identity is a safe proxy for CLI response capacity. Bounded identity/MSP responses already provide the MCU family and configuration state; API 1.46+ `MSP2_SENSOR_CONFIG_ACTIVE` provides version-specific detected sensor IDs, and API 1.47+ `MSP2_MCU_INFO` provides the MCU name. | Tagged Betaflight source audit and Airframe package/runtime tests, 2026-08-07 | A | Betaflight 4.3 through 2026.6; all Airframe Bluetooth routes | Build the hardware baseline from bounded MSP on every connection. Attempt CLI `env/status` enrichment only on a connection class known to tolerate large continuous responses, currently USB; never infer BLE safety from BF version or board/module name. | implemented and automated; live hardware display acceptance pending |
| FCC-014 | A segmented capture can safely replay all source-catalog setting and profile assignments through a CLI batch, but it cannot honestly be called a complete `dump all` replacement. The retained MSP responses preserve many additional structured values for a version-specific restore mapper, while resource/timer/DMA assignments have no bounded read endpoint on affected Betaflight versions; the only CLI listing can itself exceed the verified BLE continuous-response limit. Upstream `dump all` starts a batch and emits `save` before ending it, allowing batch errors to block persistence. | Tagged Betaflight CLI/MSP source audit and Airframe package tests, 2026-08-07 | A | Betaflight 4.3 through 2026.6 over constrained Bluetooth bridges | Make the setting/profile portion directly replayable, keep raw structured evidence, and preserve batch-error safety. Do not promise an exact structural restore until every required section has a hardware-safe capture and version-specific write mapping. | setting/profile restore implemented; complete structural restore pending |
| FCC-015 | A live SpeedyBee Adapter 3 Direct import showed that universal segmented configuration time was dominated by serialized request round trips rather than bytes. About 1,089 bounded requests produced 37,612 bytes in roughly 82 seconds; typical responses were 40–70 bytes, the observed maximum was 242 bytes, and most request/reply cycles took about 60–120 ms. The following compressed 360,448-byte FlashFS transfer took 50 seconds, opened as two logs with no header-level reader issues, and the document was materialized and reopened successfully. | Airframe Debug unified log and live user operation, 2026-08-07 | A | SpeedyBee Adapter 3 Direct, tested FC/firmware combination | Use the resolved ABF0/ABF1/ABF2 profile's complete-dump path and retain segmented capture only for constrained or unknown layouts. Continue reducing bounded request count for the shared-`FFE1` fallback. | profile-based optimization implemented; live revalidation pending |

## Configurator and Protocol Findings

Reference repository: `upstreams/betaflight-configurator/` at pinned commit `14a050b7b57b4addadc209e5b67b3cfd9fdef943`. Known SpeedyBee UART profiles include FF00/01/02, V1 1000/01/02, and V2 ABF0/1/2. Configurator's CC2541 entry uses service `FFE0`, write `FFE1`, and notify `FFE2`; Airframe additionally supports the hardware-observed shared `FFE1` layout under the same service without manufacturer matching. Adapter 3 tests showed CoreBluetooth callbacks without a recognized advertised service/name, matching Configurator's connect-then-discover strategy.

The standard handshake requests API version, FC variant, FC version, board info, and build info. FlashFS uses `MSP_DATAFLASH_SUMMARY` 70, `READ` 71, and `ERASE` 72; erase originally remained disabled. Betaflight 4.5.4 added framed CLI backports, while 4.5.3 and older require interactive CLI. A tested 4.5.1 controller emitted a 39,370-byte/1,542-line dump then rebooted on `exit`; a newer framed controller returned 39,084 bytes/1,514 lines and restored MSP after ETX.

## macOS Hardware Validation

Tests on 2026-07-23 used a SpeedyBee F405 V5 at `/dev/cu.usbmodem0x80000001`. Directly mutating the bridged matching dictionary from `IOServiceMatching` caused optimized ARM64 `EXC_BAD_ACCESS`; matching `IOSerialBSDClient` then filtering services avoided it. `MSP_BUILD_INFO` revision may be arbitrary ASCII such as `norevis`, not necessarily a hash.

A full 8 MiB FlashFS download completed through 2,048 sequential 4,096-byte reads in about 132 seconds (~62 KiB/s). The modern seven-byte request contains address, length, and compression flag; the response carries address, actual length, compression type, and data. Maximum data is 4,096 bytes inside an MSP v1 jumbo response.

Adapter 3 exposed ABF0/ABF1/ABF2 after UUID normalization. Large uncompressed BLE reads were unreliable; 512-byte reads were stable. Compressed reads operate on complete 256-byte source chunks, can validly decode zero bytes when the budget is insufficient, and used fixed-tree Huffman encoding. A 400-byte budget survived 1,577 responses/837,120 decoded bytes but throughput stayed near 3.2 KiB/s.

On 2026-08-06, the pre-change connection to `TMOTORFC_00EUV` discovered `AE00,FFE0`, selected the service-only CC2541 candidate, then found only `FFE1` and failed `characteristicUnavailable` because it required `FFE2`. The implementation now discovers the union of characteristics for every available candidate service and resolves the complete profile by UUID plus write/notify properties. Live validation consistently confirmed that Airframe resolves `FFE0 UART`, enables its shared `FFE1` notification channel, and writes the first MSP request. After UART6 MSP was enabled, one app session completed every identity stage and read a ready 132,841,472-byte dataflash with 38,912 bytes used, while another timed out at API version. The standalone probe then proved `FFE1` carries unframed MSP and isolated a first-request race: ten of ten sessions succeeded with a short post-notification delay or retry, including a 512-byte dataflash read. Airframe now gives only the first idempotent API-version request one timeout retry; all later retry policies remain unchanged. A subsequent app run downloaded all 38,912 used bytes and saved/reopened them without mutation; an independent uncompressed read matched the saved first 1,024 bytes exactly and confirmed that the only available FlashFS segment was already malformed on the controller. Using write-with-response did not fix the startup race and was reverted. `AE01`/`AE02` under `AE00` remain excluded because the public JieLi SDK identifies them as the module OTA channel. `FF02`/`FF03` under `FFE0` are undocumented and unnecessary for the validated MSP path.

The damaged `fixtures/logs/Faulty/LOG00004.bfl` fixture established that its real log end occurs at byte 744986 inside an 8 MiB dump; truncating the scanner segment there changes duration from 438.463 s to 1.261 s. It also exposed invalid/inverted graph-gap endpoints, which must be normalized before constructing ranges.

## iPadOS, MSC, and Helpers

Full platform feasibility is in [Apple Platforms and CI](APPLE_PLATFORMS_AND_CI.md) and [`USB-CDC-ACM-IPADOS.md`](USB-CDC-ACM-IPADOS.md). Betaflight's current MSC command is `msc`; MSP reboot modes 2/3 can request MSC but still require a transport. MSC is a reboot path checked early at boot, not a live switch. A configured physical MSC button is optional and often absent.

A helper attached to an MSP-enabled FC UART can download FlashFS without USB host. If it attaches to the FC USB port, it needs at least CDC-ACM host support. ESP32-S3 is the practical family: Espressif ESP32-S3-USB-OTG is the lowest-risk host validation board but lacks the preferred USB-C connector; Olimex ESP32-S3-DevKit-Lipo better matches USB-C/LiPo but host-role/VBUS behavior needs validation; LilyGO T-Display-S3 and SparkFun Thing Plus ESP32-S3 are higher-risk because documentation emphasizes device/power use.

## Specialized SpeedyBee Research

Detailed packet captures, protocol hypotheses, and experiment logs live in `SPEEDYBEE_REVERSE_ENGINEERING.md`. They are deliberately not duplicated here.

## Source Register

| Source ID | Title | Author/project | Version/date | URL or local path | Accessed | Notes |
|---|---|---|---|---|---|---|
| BFC | Betaflight Configurator | Betaflight | commit `14a050b7b57b4addadc209e5b67b3cfd9fdef943` | `upstreams/betaflight-configurator/` | 2026-08-01 | Pinned read-only reference |
| BF-SRC | MSP, VCP, MSC, and Blackbox source | Betaflight | tag `2026.6.1`, commit `6dbc4218fd6bc33bf16ea32c670304d4f89321d5` | `upstreams/betaflight/src/main/` | 2026-08-05 | Primary stable firmware evidence; MSP API remains 1.48 and dataflash wire behavior is unchanged |
| AF-HW | Airframe hardware validation notes | Airframe research | 2026-07-23 through 2026-07-24 | Original `.agents/RESEARCH.md` history and test fixtures | 2026-08-01 | Reproducible local observations |
| SB | SpeedyBee reverse-engineering notebook | Airframe research | active | `knowledge/SPEEDYBEE_REVERSE_ENGINEERING.md` | 2026-08-01 | Specialized lab notebook |
| JL-OTA | iOS JL OTA SDK BLE discovery flow | JieLi Technology | current public repository, accessed 2026-08-06 | https://gitee.com/Jieli-Tech/iOS-JL_OTA/blob/master/README.md | 2026-08-06 | Identifies `AE00` with `AE01` writes and `AE02` notifications as the module OTA channel |
| TM-GUIDE | How to Connect T-Hobby Flight Controller to Bluetooth | T-Hobby | 2025-12-17 | https://www.t-hobby.com/blog/how-to-connect-t-hobby-flight-controller-to-bluetooth | 2026-08-06 | Requires MSP on the last port/UART6 and warns reflashing may disable it |
| TM-F7-SE | Velox F7 SE Full-Feature Flight Controller | T-Hobby | current product page, accessed 2026-08-06 | https://www.t-hobby.com/products/tmotor-velox-f7-se-flight-controller | 2026-08-06 | Officially advertises wireless Bluetooth control through the SpeedyBee mobile app |
