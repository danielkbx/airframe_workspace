{
  "name": "airframe-website",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "build": "node scripts/cli.mjs build",
    "build:production": "node scripts/cli.mjs build --production",
    "test": "node --test tests/*.test.mjs",
    "validate": "node scripts/cli.mjs validate"
  },
  "engines": {
    "node": ">=20"
  }
}
